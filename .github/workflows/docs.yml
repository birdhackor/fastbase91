name: Docs

on:
  push:
    branches: [main]
    paths:
      - docs/**
      - zensical.toml
      - .github/workflows/docs.yml
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/configure-pages@45bfe0192ca1faeb007ade9deae92b16b8254a0d # v6
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.13"
      - name: Install zensical
        run: pip install "zensical==0.0.64"
      - name: Build documentation
        run: zensical build --clean
      - name: Verify generated site
        shell: bash
        run: |
          set -euo pipefail
          if [[ ! -f site/index.html ]]; then
            echo "error: zensical did not produce site/index.html" >&2
            exit 1
          fi
          mapfile -t html_files < <(find site -type f -name '*.html' -print)
          if (( ${#html_files[@]} == 0 )); then
            echo "error: zensical did not produce any HTML files in site/" >&2
            exit 1
          fi
          echo "verified ${#html_files[@]} HTML files in site/"
      - uses: actions/upload-pages-artifact@fc324d3547104276b827a68afc52ff2a11cc49c9 # v5
        with:
          path: site
      - uses: actions/deploy-pages@368f82528645a54fb793d4d04e342629a3f51346 # v5
        id: deployment
